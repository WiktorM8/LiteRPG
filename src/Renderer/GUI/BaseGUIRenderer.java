package Renderer.GUI;

import Game.GameManager;
import Renderer.BaseRenderer;
import Renderer.RenderSystem;

public abstract class BaseGUIRenderer extends BaseRenderer {
    public BaseGUIRenderer(RenderSystem renderSystem) {
        super(renderSystem);
    }
}
