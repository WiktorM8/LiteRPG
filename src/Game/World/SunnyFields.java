package Game.World;

import Entity.Player;

public class SunnyFields extends BaseWorld {

    public SunnyFields(Player player) {
        super(player); // Pass null or a Player instance if needed
        // Initialize the SunnyFields world
        // Set up the environment, entities, and other properties specific to SunnyFields
        initializeWorld();
    }

    private void initializeWorld() {
        // Load resources, set up terrain, and create entities for SunnyFields
        // This method can be expanded to include specific features of the SunnyFields world
    }
}
